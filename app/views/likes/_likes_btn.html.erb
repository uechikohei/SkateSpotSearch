          <%# unless分岐：current_userが投稿したパターン（true)== %>
          <% unless current_user == post.user %>

          <%# current_userのいいねが有る(true)%>
                <% if current_user.already_liked?(post) %>
                  いいね有
                  <%# deleteメソッド使用、id=nillエラー防ぐため@maps%>
                  <%= button_to 'いいねを取り消す', map_like_path(post, @maps), method: :delete %>
                <%# current_userのいいねが無い(false)%>
                <% else %>
                  いいね無
                    <%= button_to 'いいね', map_likes_path(post) %>
                <% end %>

          <%# unless分岐：current_userが投稿していないパターン(false)%>
          <% else %>

          <% end %>