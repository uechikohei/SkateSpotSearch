$("#items").append("<%= j(
  render partial: 'maps/mapscard', collection: @search_maps, as: "post") %>");

$("#more_link").replaceWith("<%= j(
  link_to_next_page(@search_maps, 'つづきを見る', remote: true, class: "btn filled", id: 'more_link')) %>");