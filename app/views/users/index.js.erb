$("#items").append("<%= escape_javascript(render partial: 'users_card', locals: { user: @users }) %>");
$("#more_link").replaceWith("<%= escape_javascript(link_to_next_page(@search_feeds, 'つづきを見る', remote: true, id: 'more_link', class: 'btn btn-warning btn-lg')) %>");