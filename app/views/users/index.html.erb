<h1 class="font-weight-bold text-center mb-5">ユーザー検索</h1>
<%= render "/shared/search_form_user"%>
<div class="container">
    <div class="row row-cols-1 row-cols-md-3">
        <div id="items">
            <%= render partial: 'users', locals: { user: @users }, as: "user" %>
        </div>
    </div>
</div>

<%= link_to_next_page @search_feeds, 'つづきを見る', remote: true, id: 'more_link' %>